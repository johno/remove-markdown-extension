'use strict';

var assert = require('assert');
var removeMarkdownExtension = require('..');

describe('remove-markdown-extension', function() {

  it('should do something awesome', function() {
    assert.equal(removeMarkdownExtension(), true);
  });
});
